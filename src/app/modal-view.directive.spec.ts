import { ModalViewDirective } from './modal-view.directive';

describe('ModalViewDirective', () => {
  it('should create an instance', () => {
    const directive = new ModalViewDirective();
    expect(directive).toBeTruthy();
  });
});
